@model ImageOptimization.ViewModels.SourceImageViewModel

@{
    ViewBag.Title = "Details";
}

<p>
  @Html.ActionLink("« Back to List", "Index")
</p>

<div class="img-detail">
  <img class="img" src="@Model.ServerPath" alt="@Model.AltText" width="@Model.Width.ToString()" height="@Model.Height.ToString()"/>
</div>

<h4>@Model.FileName</h4>
<hr />
<p>More info coming</p>

<p>
  @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
  @Html.ActionLink("Delete", "Delete", new { id = Model.ID })
</p>
